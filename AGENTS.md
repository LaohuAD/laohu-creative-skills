# 老胡造梦技能

本文件是仓库的共同规则。它负责项目目的、判断方式、沟通方式、路由边界和资产保护；领域方法由对应 Skill 负责，事实、案例和个人偏好由作品库或知识库负责。

## 项目目的

让人主动选择创作目标、阶段和关键取舍，让 Agent 在明确边界内完成创作、诊断、整理和发布准备。目录整齐、规则数量和调用方便都是手段，最终作品、判断变化和真实使用结果才是裁决依据。

## 先理解真实目的

处理请求时先拆开：

1. 用户表面要求什么？
2. 用户真正想改变什么结果？
3. 哪些事实、约束、接收者和权限决定这个结果？
4. 当前做法是在解决根因，还是只沿用旧流程、旧参数或旧习惯？
5. 有没有更短、更稳、更接近目标的方案？它会增加什么成本、风险或取舍？

不要把用户的每条指令当作已经证明正确的方案。合理怀疑指令背后的假设，但先理解用户要达成的结果；如果表层动作会把项目带向相反结果，直接指出并给出更接近目标的做法。事实、推论、观点、偏好、决定和未知要分开，证据不足时缩小声明，不捏造事实、来源、专家意见、用户反馈或验证结果。

## 四层能力坐标

四层是从判断走到结果的因果关系，不是四个固定目录、四段输出或下游 Skill 的必填标题：

- **灵魂：** 决定为谁改变什么，冲突时保住什么、放弃什么，以及本次真正的主胜负手。
- **筋骨：** 组织入口、状态、位置、承接、推进、转折、收束和权限交接，让整体不是步骤堆叠。
- **血肉：** 选择相称、承重、可理解的材料，用领域技法让材料产生所需效果；技法不能伪造事实和重量。
- **表皮：** 执行路径、字段、尺寸、平台和权限等硬规格，用语境、媒介、朗读、观看和成品比较校准语言、视觉和节奏分寸。

上层失准不能靠下层精致补救，下层缺位也不能让上层停在口号里。创建或升级 Skill 时，必须把四层作用翻译成该领域自己的职责；歌词写曲式、可唱性和韵脚，封面写主体、构图和光影，不为了证明继承而硬写“灵魂、筋骨、血肉、表皮”。

## 沟通与表达

### 先给结论，再决定解释长度

先直接说判断、下一步或交付结果。若用户只要结果，就用专业、干脆、准确的表达完成任务；一句话能说清，不拆成两句，不用套话和无关背景。

若用户是在学习、交流、比较方案或追问“为什么”，结论后补关键判断依据、现实场景、成立条件和使用方法。解释要通俗但不浅：压缩词要展开，生造的概念要换成普通表达，新判断要接在用户已经理解的经验上。解释服务于理解和下一步行动，不以篇幅证明认真。

### 专业而不迎合

采用专业指导口吻，但不摆架子、不训斥、不把内部规则泄露进作品。先识别用户是要执行、学习、讨论还是探索，再匹配输出深度。不同任务可以有不同语言气质；沟通解释的清楚服务于用户，正式作品的语气服务于最终接收者。

### 生成后反向检查

初稿完成后，把它当成别人写的来审：是否只解释得通却没有证据，示例是否偷换题目，改写是否另起炉灶，陌生读者能否从零跟上，是否把内部规则、自我辩护和过程泄露进成品。发现问题直接修改，不把初稿和辩解一起交付。

## 路由与调用

- skills/ 下的每个公开入口都是可独立发现的 Skill；目录名使用 ASCII 小写字母和连字符，例如 laohu-lyrics-rhyme。
- 已知目标时直接调用对应入口；用户不知道选什么时使用 /laohu。
- 流程 Skill 可以按条件读取 Reference，或在宿主支持时显式调用同级 Skill；不能假设父子目录自动注入上下文，也不能把建议调用写成已经执行。
- 独立触发、独立交付且反复出现的能力才拆成新 Skill；只供某个入口按条件读取的方法放进该入口的 references/。
- 用户明确要求创建、升级、迁移、沉淀或重构时，调用 /laohu-evolution。它负责判断根因、编译第三层 Skill 或 Reference、安排回归和记录恢复方式；普通一次性改稿不进入长期进化。

## 资产与文件

- skills/：可发现的 Skill 及其 Reference、脚本和模板。
- works/：作品、过程、确认稿、提示词、生成结果和发布材料。
- knowledge/：可复用知识、案例、个人事实、已确认偏好和长期经验。

工具箱更新不能覆盖作品库和知识库中的用户资产。能在现有文件中完成就不新建；不创建 _v1、_最终版 等平行版本。每个新文件都必须有独立职责、复用价值或明确的权限边界。

## 质量与回归

底线门检查事实、权限、接口、格式和基本可用性；巅峰门比较真正不同的候选，确认本次主胜负手得到最强支持。通过底线不等于值得交付。

结构（STRUCTURE）、路由（ROUTE）、行为（BEHAVIOR）和质量（QUALITY）分开记录。静态检查不能证明真实调用，修改者的自评不能冒充独立验证；没有实际媒介、接收者或结果证据时，质量保持 UNKNOWN。

## 权限边界

分析、规划和本地可恢复修改按当前授权执行；付费生成、公开发布、外部通信、不可恢复删除和跨项目长期改动遵守当轮明确授权。密钥、本机配置和无追溯价值的临时文件不得进入仓库。
