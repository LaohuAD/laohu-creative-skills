# 老胡造梦技能

本仓库是老胡创作能力的独立 Skill 工具箱。它负责提供可主动调用的创作入口、专项能力和可复用方法；具体作品、生成结果和私人素材按职责保存到作品库或知识库。

## 当前阶段

当前只建立目录骨架，不预先创建任何 `SKILL.md`、Reference、模板或工具。后续新增能力时，先确定它是公开入口、流程控制器、专项能力，还是仅供入口读取的方法资料，再放入对应位置。

## 目录规则

- `skills/`：所有可发现的 Skill 入口。需要被不同 Agent 独立调用的能力，放在这里的同一级目录，不依赖嵌套目录自动继承。
- `works/`：作品项目、创作过程、确认稿、提示词、生成结果和发布材料。
- `knowledge/`：可复用知识、案例、经验、个人偏好和长期资产。
- 每个公开入口使用 `laohu-{name}` 命名；`laohu` 是总入口。
- 目录名和入口名使用 ASCII 小写字母与连字符，不使用中文、空格或版本后缀。
- 方法、案例和工具只在确有职责差异或需要独立复用时拆分；不为一次性过程创建平行版本文件。

## Skill 调用边界

- `/laohu` 负责用户不知道该选择什么时的路由与任务澄清。
- 已明确目标时，直接调用对应的一级 Skill。
- 大流程 Skill 可以按条件读取 Reference，或在宿主支持时显式调用同级专项 Skill；不能假设父子目录会自动加载上下文。
- 子 Skill 不可用时必须如实说明，不能把推荐调用写成已经执行。
- 每个 Skill 都要写清触发条件、输入、输出、必须保留的内容、停止条件和权限边界。

## 资产保护

工具箱方法的更新不能覆盖 `works/` 中的作品，也不能覆盖 `knowledge/` 中的个人资料、案例和已确认偏好。任何可复用反馈先判断是当前作品修改、当前任务规则、项目级规则还是待验证经验，再决定是否沉淀。
