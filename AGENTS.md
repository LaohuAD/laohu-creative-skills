# 老胡造梦技能

本文件规定项目共同目标、判断与授权边界、任务路由、资产归属和交付责任。具体任务的执行步骤、格式规格、专业方法及验证操作，由负责该任务的 Skill 和其按条件读取的 Reference 维护。顶层保留调用条件、必要交接和共同约束，不重复展开执行细则。

## 项目目标

让人主动决定创作目标、阶段和关键取舍，让 Agent 在清楚的边界内完成创作、诊断、整理、进化和发布准备。目录、文件数量和规则长度都不是成果；能否改变下一步判断和最终作品才是成果。

## 共同运行规则

处理本项目任务时先读取 [能力运行约定](docs/runtime.md)，沿用其中的任务状态、意图判断、授权、路由、交接和通知要求。它也是各 Skill 在项目外调用时的共同入口，相关判断只在那里维护；本文件保留仓库维护、验证和资产职责。

## 验证

修改 Skill、Reference、项目工具、引用、目录或版本前，实施负责人必须读取 [项目检查与版本维护](docs/maintenance.md) 的对应要求，并在交付前执行适用检查；准备版本时读取该文档的发布流程。具体命令与版本操作只在那里维护，不因完成本地修改自动提交、推送或发布。

官方版本查询和完整项目更新由 `laohu-update` 承接，保护本地提交及暂存、未暂存、未跟踪和被忽略资产；冲突先给具体方案、获得确认后应用。更新操作按该入口及其引用执行，能力改进由 `laohu-evolution` 负责，不互相替代。

分别记录四类证据，并使用 `PASS / OBSERVE / FAIL / UNKNOWN`：

- `STRUCTURE`：frontmatter、路径、引用、格式和目录正确；
- `ROUTE`：真实请求能找到正确入口和必要 Reference；
- `BEHAVIOR`：代表请求实际改变了判断、动作、中间产物或停止点；
- `QUALITY`：最终接收者、真实媒介、专家或使用结果证明质量变化。

静态检查最多证明结构；同一修改者的自评最多证明观察到行为。没有真实媒介、接收者或结果反馈时，质量必须写 `UNKNOWN`。

完成交付时说明：改了什么、写回哪里、依据什么、验证到哪一级、哪些效果仍待验证、如何恢复。不要把密钥、本机路径、私密材料或未授权外部内容写入仓库。

## 资产与权限

- `.agents/skills/` 是 Skill 真源，`works/` 保存作品与生成结果，`knowledge/` 保存可复用事实、案例、偏好和已验证经验；同一判断只维护一处：仓库维护约束归本文件，跨入口运行约定归 `docs/runtime.md`，专业流程与细则归负责 Skill 及其按需引用；脚本、评测和界面配置随能力维护。发现重复先核对实际消费者，再合并到真源。
- 写入 `knowledge/` 时只沉淀可复用的事实、案例、偏好和已确认经验；运行时需要的结论须编译成规则，并保留决定其使用的适用条件、例外和停止条件。来源、更新时间及仅供维护追踪的资料状态、范围说明留在台账，不原样复制进 Skill 或 Reference；其中会改变执行选择的信息，须转写为相应条件和动作。待验证内容不能单独决定全局路由或硬性规则。
- 当前结果不好，不自动等于项目规则失效；先复现失败并找到最早断点。一次性修订留在当前作品，明确要求长期沉淀时才写入规则、Reference 或知识库。
- 修改前保留当前实际文件及可恢复位置，不能只以 Git 的 HEAD 代替未提交资产的备份。
- 能在现有文件中完成就不新建，不创建 `_v1`、`_最终版` 等平行版本；新文件必须有独立职责、复用价值或明确权限边界。
- 操作权限与用户资产保护沿用 `docs/runtime.md`，不因维护仓库扩大授权。
